{% extends 'base.html.twig' %}

{% block title %}Film Detail Page{% endblock %}
{% block body %}
    <h1>Film detail page</h1>
    <div>
        <span>{{ film.title }}</span><br>
        <span>{{ film.getDirector() }}</span><br>
        <span>{{ film.getReleaseDate() }}</span><br>
    </div>
{% endblock %}
